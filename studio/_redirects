# Sanity Studio - serve static files first with FORCE
# These rules MUST be in studio/_redirects to have priority
/static/*  /static/:splat  200!
/*.js  /:splat  200!
/*.css  /:splat  200!
/*.png  /:splat  200!
/*.ico  /:splat  200!
/*.webmanifest  /:splat  200!
/*.svg  /:splat  200!
/*.woff  /:splat  200!
/*.woff2  /:splat  200!

# SPA fallback for studio routes (only if file doesn't exist)
/*  /index.html  200
